# Финальный аудит — версия 2

Проверка не по наличию кода, а по фактическому поведению. Отметка «проверено» означает, что
поведение подтверждено автоматическим тестом либо запуском приложения в браузере (или тем и другим).

Прогон верификации на момент аудита: `npm run typecheck` — без ошибок, `npm run lint` — 0 ошибок и
предупреждений, `npm run test` — 99 тестов в 9 файлах пройдены, `npm run build` — сборка успешна
(45 модулей).

## Замечания внешнего ревью и как они закрыты

| Находка | Приоритет | Что сделано | Чем подтверждено |
| --- | --- | --- | --- |
| «Принять рекомендацию» записывалось как решение оператора | P1 | Введён источник `accepted`: считается отдельно в сводке, пересчитывается при автопрогоне наравне с `auto` | `reviewFindings.test.ts` — 3 теста; UI-тест «принятая рекомендация не выдаётся за решение оператора»; в браузере: плитка «система 5 · принято 1 · оператор 0», после повторного прогона заявка снова «система» |
| Дефицит мест диагностировался как «переселение невозможно» | P1 | Статус `awaiting_capacity`, функция `classifyUnplaced`, отдельный экран с указанием, кто занимает подходящие места | `reviewFindings.test.ts` — 3 теста; UI-тест «нехватка мест объясняется как ожидание»; обновлён тест распределения при нехватке вместимости |
| Балл расходился между сводкой и карточкой | P1 | Отчёт переведён на `explainExistingAssignment`; добавлено состояние «размещение требует пересмотра» | `reviewFindings.test.ts` — 2 теста (в том числе прямое сравнение значения из отчёта и из карточки); UI-тест «размещение с истёкшим сроком помечается и в списке, и в сводке» |
| Плитка «просрочено N» считала по всему реестру | P1 | Разделены `overdueTotal` и `overdueUnplaced` | `reviewFindings.test.ts` — тест с одной просроченной размещённой и одной просроченной без места; в браузере: «из них просрочено 1» |
| После подтверждения терялось сравнение с рекомендацией | P2 | Назначение сохраняет `assignmentRecord`; карточка показывает блок «Как принято решение» | `reviewFindings.test.ts` — 2 теста; UI-тест «после ручного выбора видно, что система советовала другое» |
| Разбор «места нет» повторял общую причину | P2 | `explainNoMatch` выносит общие блокировки в корень и убирает их из вариантов; сортировка учитывает только собственные препятствия | `reviewFindings.test.ts` — 2 теста; UI-тест считает вхождения причины в панели; в браузере: причина названа один раз, у вариантов «Других препятствий нет» |
| Деструктивные действия без страховки | P2 | Двухшаговое подтверждение очистки; нечитаемое хранилище уходит в резервный ключ; полная валидация схемы вместо трёх полей | `persistence.test.ts` — 6 тестов; UI-тест «очистка реестра требует подтверждения» |
| Склонение «прежних жильцов», ключ localStorage строкой, вкладки без роли `tab`, замечания для уже занятого места | P3 | Исправлено: `plural`, импорт `STORAGE_KEY`, `role="tab"` с `aria-selected` и `tabpanel`, отдельная ветка для текущего места | UI-тест «вкладки объявлены как вкладки для скринридера»; остальное — визуальная проверка |

Ни одна находка не отклонена. Проверки, которые ревью назвало отсутствующими, добавлены: прежний
набор из 73 тестов не ловил ни одного из четырёх P1.

## P0 — обязательное

| Требование | Реализовано | Проверено | Где |
| --- | --- | --- | --- |
| Приложение запускается | да | да — dev-сервер на :5174, интерфейс работоспособен | `npm run dev` |
| Production build проходит | да | да — 45 модулей, сборка без ошибок | `npm run build` |
| Основной пользовательский сценарий работает | да | да — распределение 6 из 8 заявок вживую и в тесте | `ui/__tests__/app.test.tsx` |
| Заявки отображаются | да | да — список со статусами, включая новые | `ui/GhostList.tsx` |
| Места отображаются | да | да — таблица 6 мест со средой и ограничениями | `ui/LocationsView.tsx` |
| Matching работает | да | да — балл и лучший вариант совпадают с расчётом, сделанным до кода | `domain/matching.ts` |
| Hard constraints соблюдаются | да | да — 8 кодов, каждый покрыт тестом | `domain/__tests__/constraints.test.ts` |
| Невозможные переселения объясняются | да | да — корневая причина без повторов, ближайшие варианты | `ui/GhostDetail.tsx` → `UnplacedPanel` |
| Manual assignment валидируется | да | да — отказ на заполненном месте, подтверждение при замечаниях | `domain/assignment.ts`, `ui/ManualAssign.tsx` |
| Capacity учитывается корректно | да | да — занятость не превышает вместимость | `assertCapacityInvariant`, тесты назначения |
| Deadline учитывается корректно | да | да — блокируется даже при `confirmed: true`; для размещённых включается пересмотр | `domain/dates.ts`, `reviewFindings.test.ts` |
| Основные edge cases проверены | да | да — см. «Состояния интерфейса» | тесты + ручная проверка |
| Нет падающих тестов | да | да — 99 из 99 | `npm run test` |
| Нет очевидно сломанных пользовательских путей | да | да — пройдены все вкладки и все три пути назначения | ручная проверка в браузере |

## P1 — важное

| Требование | Реализовано | Проверено | Где |
| --- | --- | --- | --- |
| Понятный UX | да | да — четыре вкладки, список + деталь, статусы бейджами | `src/ui/` |
| Human-readable explanations | да | да — причины и разбор балла из данных матчинга; след решения после подтверждения | `MatchExplanation.tsx`, `GhostDetail.tsx` |
| Глобальное распределение | да | да — приоритет защищает заявку с единственным вариантом | `domain/globalAssignment.ts` |
| Error/empty-state UX | да | да — пустой реестр, повреждённое хранилище с резервной копией, ErrorBoundary | `RequestsView.tsx`, `persistence.ts`, `ErrorBoundary.tsx` |
| AI Worklog | да | да — отдельная вкладка, включая этап внешнего ревью | `ui/WorklogView.tsx` |
| README | да | да — раздел о различиях версий, архитектура, алгоритм, edge cases | `README.md` |
| Аккуратная архитектура | да | да — домен не импортирует React ни в одном файле | `src/domain/` |

## Доменные инварианты

| Инвариант | Проверено | Чем |
| --- | --- | --- |
| Блокирующий конфликт нельзя компенсировать баллом | да | тест: у несовместимого места `score === 0` при `breakdown.total === 100` |
| Занятость никогда не превышает вместимость | да | тест последовательных назначений + `assertCapacityInvariant` |
| Просроченная заявка не назначается никуда | да | тест с `confirmed: true` — назначение всё равно отклонено |
| Ручное и автоматическое назначение проходят одну проверку | да | обе ветки вызывают `validateAssignment` внутри `assign()` |
| Назначение с предупреждениями требует подтверждения | да | тест: без `confirmed` — отказ, с `confirmed` — успех |
| Распределение детерминировано | да | тест: повторный прогон даёт тот же результат |
| Решение оператора не переписывается, решение системы пересматривается | да | тесты `skippedManual` для `manual` и пересчёта для `accepted` |
| Заявка не блокирует сама себя на своём же месте | да | тест повторной проверки собственного места при полной занятости |
| Сводка и карточка не могут разойтись в оценке | да | тест сравнивает значение из отчёта со значением из `explainExistingAssignment` |

## Состояния интерфейса

| Состояние | Проверено | Как |
| --- | --- | --- |
| Empty state | да | подтверждение очистки → сообщение и загрузка демо-данных (браузер + тест) |
| Переселение невозможно | да | Кальцифер и Марфа: корневая причина один раз, варианты со своими препятствиями |
| Ждёт свободного места | да | UI-тест с одним местом на две заявки; в браузере на реестре из двух заявок и одного места: «размещено 1, ждут свободного места 1», панель называет место и того, кто его занимает |
| Размещение требует пересмотра | да | UI-тест и доменный тест: балл 0 одинаково в списке, карточке и сводке |
| Location full | да | Особняк Тумановых: «заполнено», ручной выбор запрещён |
| Manual conflict | да | выбор заполненного места: причина и серьёзность показаны |
| Подтверждение при замечаниях | да | двухшаговое подтверждение, след решения сохраняется |
| Generic error | да | испорченный localStorage → сообщение + резервная копия (тест `persistence.test.ts`) |
| Ошибка рендеринга | да | ErrorBoundary фактически сработал в первой версии при `ReferenceError` во время HMR |

## Что осталось за рамками

- Сценарий отказа localStorage при записи (переполнение квоты) обрабатывается в коде, но
  искусственно не воспроизводился.
- Тестов на компоненты `ReportView` и `LocationsView` по отдельности нет — они проверены
  сценарно через `app.test.tsx` и вручную.
- Фаззинг, которым ревью проверяло неуязвимость домена, в репозиторий не добавлен: он остаётся
  внешним инструментом проверки, а не частью поставки.
